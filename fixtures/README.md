# Fixtures

Sample prose for the emphasis pass. The skill writes `report.md`;
these files are the input, not a command-line corpus.

| Folder | What it shows |
| --- | --- |
| `cancel-friday/` | The teaching sentence, plus *only* and negation scope |
